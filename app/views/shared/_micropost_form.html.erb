<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Componer nuevo micropost..." %>
  </div>
  <%= f.submit "Postear", class: "btn btn-large btn-primary" %>
<% end %>


